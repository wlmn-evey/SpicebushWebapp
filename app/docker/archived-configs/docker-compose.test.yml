services:
  # Simple Astro app test
  app:
    build:
      context: .
      dockerfile: Dockerfile.simple
    ports:
      - "4321:4321"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
    restart: unless-stopped

networks:
  default:
    driver: bridge