---
import AdminLayout from '@layouts/AdminLayout.astro';
import { db } from '@lib/db';

const settings = await db.content.getAllSettings();
const savedKey = Astro.url.searchParams.get('saved');
const errorMessage = Astro.url.searchParams.get('error');
const previewMode = Astro.cookies.get('sbms-preview-mode')?.value === 'site' ? 'site' : 'coming-soon';

const stringifyValue = (value: unknown): string => {
  if (value === null || value === undefined) return '';
  if (typeof value === 'string') return value;
  return JSON.stringify(value);
};

const boolFrom = (value: unknown): boolean => {
  if (typeof value === 'boolean') return value;
  if (typeof value === 'number') return value === 1;
  if (typeof value === 'string') {
    return ['true', '1', 'yes', 'on'].includes(value.trim().toLowerCase());
  }
  return false;
};

const comingSoonEnabled = boolFrom(settings.coming_soon_enabled);
const comingSoonMessage = stringifyValue(settings.coming_soon_message);
const comingSoonLaunchDate = stringifyValue(settings.coming_soon_launch_date);
const donationLink = stringifyValue(settings.donation_external_link);
const enrollmentLink = stringifyValue(settings.enrollment_external_link);
const sortedEntries = Object.entries(settings).sort(([a], [b]) => a.localeCompare(b));
---

<AdminLayout title="Manage Settings">
  {savedKey && (
    <p class="mb-4 rounded-lg border border-green-200 bg-green-50 p-3 text-sm text-green-800">
      Saved setting: <strong>{savedKey}</strong>
    </p>
  )}

  {errorMessage && (
    <p class="mb-4 rounded-lg border border-red-200 bg-red-50 p-3 text-sm text-red-800">
      {errorMessage}
    </p>
  )}

  <div class="space-y-6">
    <section class="rounded-xl border border-gray-200 bg-white p-5 shadow-sm">
      <h2 class="text-lg font-semibold mb-3">Admin Preview Toggle</h2>
      <p class="text-sm text-earth-brown/80 mb-4">
        Current preview mode: <strong>{previewMode === 'site' ? 'Full Site' : 'Coming Soon'}</strong>
      </p>
      <form
        method="post"
        action={`/api/admin/preview-mode?mode=${previewMode === 'site' ? 'coming-soon' : 'site'}&redirect=${encodeURIComponent('/admin/settings')}`}
      >
        <button type="submit" class="rounded-lg bg-forest-canopy px-4 py-2 font-semibold text-white hover:bg-moss-green">
          {previewMode === 'site' ? 'Switch To Coming Soon View' : 'Switch To Full Site View'}
        </button>
      </form>
    </section>

    <section class="rounded-xl border border-gray-200 bg-white p-5 shadow-sm">
      <h2 class="text-lg font-semibold mb-3">Core Settings</h2>
      <div class="space-y-4">
        <form method="post" action="/api/admin/settings" class="space-y-2">
          <input type="hidden" name="key" value="coming_soon_enabled" />
          <input type="hidden" name="redirectTo" value="/admin/settings?saved=coming_soon_enabled" />
          <label class="block text-sm font-medium">
            Coming Soon Enabled
            <select name="value" class="mt-1 w-full rounded-lg border border-gray-300 px-3 py-2">
              <option value="true" selected={comingSoonEnabled}>true</option>
              <option value="false" selected={!comingSoonEnabled}>false</option>
            </select>
          </label>
          <button type="submit" class="rounded-lg bg-forest-canopy px-4 py-2 text-sm font-semibold text-white hover:bg-moss-green">
            Save Coming Soon Toggle
          </button>
        </form>

        <form method="post" action="/api/admin/settings" class="space-y-2">
          <input type="hidden" name="key" value="coming_soon_message" />
          <input type="hidden" name="redirectTo" value="/admin/settings?saved=coming_soon_message" />
          <label class="block text-sm font-medium">
            Coming Soon Message
            <textarea name="value" rows="3" class="mt-1 w-full rounded-lg border border-gray-300 px-3 py-2">{comingSoonMessage}</textarea>
          </label>
          <button type="submit" class="rounded-lg bg-forest-canopy px-4 py-2 text-sm font-semibold text-white hover:bg-moss-green">
            Save Message
          </button>
        </form>

        <form method="post" action="/api/admin/settings" class="space-y-2">
          <input type="hidden" name="key" value="coming_soon_launch_date" />
          <input type="hidden" name="redirectTo" value="/admin/settings?saved=coming_soon_launch_date" />
          <label class="block text-sm font-medium">
            Coming Soon Launch Date
            <input type="text" name="value" value={comingSoonLaunchDate} class="mt-1 w-full rounded-lg border border-gray-300 px-3 py-2" />
          </label>
          <button type="submit" class="rounded-lg bg-forest-canopy px-4 py-2 text-sm font-semibold text-white hover:bg-moss-green">
            Save Launch Date
          </button>
        </form>

        <form method="post" action="/api/admin/settings" class="space-y-2">
          <input type="hidden" name="key" value="donation_external_link" />
          <input type="hidden" name="redirectTo" value="/admin/settings?saved=donation_external_link" />
          <label class="block text-sm font-medium">
            Donation External Link (future)
            <input type="text" name="value" value={donationLink} class="mt-1 w-full rounded-lg border border-gray-300 px-3 py-2" />
          </label>
          <button type="submit" class="rounded-lg bg-forest-canopy px-4 py-2 text-sm font-semibold text-white hover:bg-moss-green">
            Save Donation Link
          </button>
        </form>

        <form method="post" action="/api/admin/settings" class="space-y-2">
          <input type="hidden" name="key" value="enrollment_external_link" />
          <input type="hidden" name="redirectTo" value="/admin/settings?saved=enrollment_external_link" />
          <label class="block text-sm font-medium">
            Enrollment External Link (future)
            <input type="text" name="value" value={enrollmentLink} class="mt-1 w-full rounded-lg border border-gray-300 px-3 py-2" />
          </label>
          <button type="submit" class="rounded-lg bg-forest-canopy px-4 py-2 text-sm font-semibold text-white hover:bg-moss-green">
            Save Enrollment Link
          </button>
        </form>
      </div>
    </section>

    <section class="rounded-xl border border-gray-200 bg-white p-5 shadow-sm">
      <h2 class="text-lg font-semibold mb-3">All Settings</h2>
      <div class="space-y-4">
        {sortedEntries.map(([key, value]) => (
          <form method="post" action="/api/admin/settings" class="space-y-2 rounded-lg border border-gray-200 p-3">
            <input type="hidden" name="key" value={key} />
            <input type="hidden" name="redirectTo" value={`/admin/settings?saved=${encodeURIComponent(key)}`} />
            <label class="block text-sm font-medium">{key}</label>
            <textarea
              name="value"
              rows="2"
              class="w-full rounded-lg border border-gray-300 px-3 py-2 font-mono text-sm"
            >{stringifyValue(value)}</textarea>
            <button type="submit" class="rounded-lg bg-forest-canopy px-3 py-1.5 text-sm font-semibold text-white hover:bg-moss-green">
              Save {key}
            </button>
          </form>
        ))}
      </div>
    </section>

    <section class="rounded-xl border border-gray-200 bg-white p-5 shadow-sm">
      <h2 class="text-lg font-semibold mb-3">Create New Setting</h2>
      <form method="post" action="/api/admin/settings" class="space-y-3">
        <input type="hidden" name="redirectTo" value="/admin/settings?saved=new" />
        <input type="text" name="key" placeholder="new_setting_key" required class="w-full rounded-lg border border-gray-300 px-3 py-2" />
        <textarea name="value" rows="3" placeholder="Setting value (string or JSON)" class="w-full rounded-lg border border-gray-300 px-3 py-2"></textarea>
        <button type="submit" class="rounded-lg bg-forest-canopy px-4 py-2 font-semibold text-white hover:bg-moss-green">
          Create Setting
        </button>
      </form>
    </section>
  </div>
</AdminLayout>

