---
import { getTourLink } from '@lib/tour-settings';

const tourLink = await getTourLink();

if (tourLink === '/admissions/schedule-tour') {
  return Astro.redirect('/contact');
}

return Astro.redirect(tourLink);
---
