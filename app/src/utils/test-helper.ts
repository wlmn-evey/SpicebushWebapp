/**
 * Simple test helper utility to verify path alias resolution
 */

export function testHelper(message: string): string {
  return `Test helper: ${message}`;
}

export const TEST_CONSTANT = 'Path alias @utils is working correctly';