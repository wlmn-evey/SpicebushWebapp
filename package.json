{
  "scripts": {
    "dev": "netlify dev",
    "build": "astro build",
    "deploy": "netlify deploy"
  },
  "dependencies": {
    "@browsermcp/mcp": "^0.1.3",
    "node-fetch": "^2.7.0"
  },
  "devDependencies": {
    "netlify-cli": "^23.5.0"
  }
}
